block content
	div.readme
		p.readme-title.text-primary
			i.fa.fa-github.fa-lg &nbsp;
			| #{author} 
			span.text-muted / 
			| #{repo}
		// != md.toHTML(contents)
		if contents
			!= marked(contents)
		else
			p.modal-title #{error}
